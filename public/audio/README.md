# Background music

Add your track here as **ambient.mp3** (loop-friendly, soft instrumental works best).

The invite loads `/audio/ambient.mp3` by default. You can change the path or volume in `src/content/const-content.js` under `audio`.
